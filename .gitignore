# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local
tmp/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env
.env.local

# Ignore problematic JS file that blocks TS build
src/components/CourseContentCreator.js

# Local Netlify folder
.netlify

# Ignore server diagnostics artifacts
server/diagnostics/*.json
server/diagnostics/*.log
*.log
!server/diagnostics/metrics.js
.env
.env.*
# Allow .env.production to be committed — it contains only PUBLIC Vite build-time values
# (Supabase anon key, API URLs). No secrets. Railway/Netlify env vars override at build time.
!.env.production
public/sw-version.json
