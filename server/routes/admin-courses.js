import express from 'express';

/**
 * @deprecated Admin course routes now live directly in server/index.js.
 * This stub persists so legacy imports resolve without side effects.
 */
const router = express.Router();

export default router;
